<div class="global">
<%= form_for(@ciudad) do |f| %>
  <% if @ciudad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ciudad.errors.count, "error") %> prohibited this ciudad from being saved:</h2>

      <ul>
      <% @ciudad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre, :required =>"true",:placeholder=>"nombre de ciudad",:autofocus =>"true" %>
  </div>
  <div class="field">
    <%= f.label :id_empresa %><br />
    <%= f.number_field :id_empresa %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>